it is already in the list, it will not be added again. This is useful for ensuring that a list contains only unique elements.

Why do we use Pydantic?
Pydantic is used to define request and response models. It automatically validates incoming data, converts types where appropriate, generates clear validation errors, and integrates with FastAPI's automatic API documentation.